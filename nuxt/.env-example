# =====================================================================
# プロジェクト識別（コンテナ名・ネットワーク用）
# =====================================================================
PROJECT_NAME=nuxt
CONTAINER_NAME=nuxt-portfolio

# ホストの UID/GID（コンテナ内のファイル所有者を合わせる）
UID=1000
GID=1000

# =====================================================================
# 複数 Nuxt プロジェクト対応
# =====================================================================
# 利用するプロジェクトのディレクトリ名（作業ディレクトリ）
# 例: app → /var/www/app、別プロジェクト app2 を利用する場合は app2
WORK_DIR=app

# =====================================================================
# Nuxt 開発サーバー
# =====================================================================
HOST_PORT=3000
CONTAINER_PORT=3000
# プレビュー用（必要に応じて）
PREVIEW_HOST_PORT=3001
PREVIEW_CONTAINER_PORT=3000

NODE_ENV=development

# =====================================================================
# MailHog（メールキャッチ）
# =====================================================================
MAILHOG_WEB_PORT=8025
MAILHOG_SMTP_PORT=1025

# =====================================================================
# Nu Html Checker (vnu)
# =====================================================================
VNU_PORT=8888

# =====================================================================
# Nuxt 設定（必要に応じて）
# =====================================================================
# NUXT_HOST=0.0.0.0
# NUXT_PORT=3000
